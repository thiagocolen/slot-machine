{
  "name": "slot-machine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "source": "src/index.html",
  "scripts": {
    "start": "npx parcel serve -p 4000",
    "build": "npx parcel build",
    "deploy": "npx parcel build --public-url \"https://thiagocolen.github.io/slot-machine/\" && npx gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^5.0.0",
    "lit-element": "^3.3.2",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "parcel": "^2.9.1"
  }
}
